version: '3'

services:
  postgres-compose:
    image: postgres
    environment:
      POSTGRES_PASSWORD: "pass"
    ports:
      - "5432:5432"
    volumes:
      - ${HOME}/postgresql/data:/var/lib/postgresql/data
    networks:
      - postgres-compose-network
    container_name: postgres-container

  pgadmin-compose:
    image: dpage/pgadmin4
    environment:
      PGADMIN_DEFAULT_EMAIL: "user@user.org"
      PGADMIN_DEFAULT_PASSWORD: "pass"
    ports:
      - "8055:80"
    depends_on:
      - postgres-compose
    networks:
      - postgres-compose-network
    container_name: pgadmin-container

networks:
  postgres-compose-network:
    driver: bridge

#Find postgre Ip to be configured in pgadmin
#docker inspect postgres-container -f "{{json .NetworkSettings.Networks }} | grep IPAddress